import time
import pytest

def test_triangulation_performance_placeholder():
    """Test de performance futur : doit durer moins de X secondes."""
    start = time.time()
    # futur appel triangulation
    time.sleep(0.01)  # placeholder
    duration = time.time() - start
    assert duration < 1.0
