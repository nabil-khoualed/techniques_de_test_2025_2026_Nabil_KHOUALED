"""
Fonctions utilitaires pour la sérialisation et la désérialisation du PointSet.

Ce module sera implémenté lors de la dernière séance.
"""

def serialize_pointset(points):
    """
    Sérialise un ensemble de points en binaire, selon le format décrit
    dans point_set_manager.yml.

    Parameters
    ----------
    points : list[tuple[float, float]]
        La liste des points à encoder.

    Raises
    ------
    NotImplementedError
        La logique n'est pas encore implémentée.
    """
    raise NotImplementedError("serialize_pointset() non implémenté pour le moment.")


def deserialize_pointset(data):
    """
    Désérialise un binaire PointSet en une liste de points (x, y).

    Parameters
    ----------
    data : bytes
        Données binaires représentant un PointSet.

    Raises
    ------
    NotImplementedError
        La logique n'est pas encore implémentée.
    """
    raise NotImplementedError("deserialize_pointset() non implémenté pour le moment.")
